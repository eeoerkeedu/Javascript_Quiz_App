//getting element variables set

var scoreboard = document.getElementById("scoreblock");
var quizBox = document.getElementById("quizblock");

//defining variables
var time = 0;
var score = 0;
var userName = "";

//user profile to track stats
var userProfile = {
  Name: userName,
  userScore: score,
  userTime: 10000,
};

//questions to be asked
var questions = [];

// Button to hide scoreboard, initiate test, and start timer

// intiate page load
function initiate() {
  //hide quiz
  //stop quiz
  //show scoreboard -- user best
}

initiate();
